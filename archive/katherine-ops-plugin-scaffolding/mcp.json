{
  "mcpServers": {
    "roam": {
      "command": "npx",
      "args": ["-y", "roam-research-mcp"],
      "env": {
        "ROAM_API_TOKEN": "${ROAM_API_TOKEN}",
        "ROAM_GRAPH_NAME": "${ROAM_GRAPH_NAME:-kvond}"
      }
    }
  }
}
